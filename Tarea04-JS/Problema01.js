// Declarando la variable para presenta la pregunta 
var creador = prompt("¿Cuál es el nombre del ‘creador’ de JavaScript?");

// Verificacion de la respuesta  por medio de IF ELSE
if (creador === "Brendan Eich") {
  console.log("¡Correcto!");
} else {
  console.log("¿No lo sabes? ¡Brendan Eich!");
}
